---
- name: Apt - Upgrade all packages to the latest version
  become: true
  apt:
    update_cache: yes
    upgrade: yes

- name: Apt - Install packages
  become: true
  apt:
    name: "{{ packages }}"
