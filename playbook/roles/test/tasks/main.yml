---
- debug:
    msg: "{{ item }}"
  with_items: "{{ packages }}"

- debug:
    msg: "{{ inventory_hostname }}"

- debug:
    msg: "{{ role_path }}"

- debug:
    msg: "{{ ansible_distribution }}"

- debug:
    msg: "{{ ansible_os_family }}"

- debug: var=ansible_facts
